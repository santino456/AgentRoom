version: "3.8"

services:
  agent-coop:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - agent-coop-data:/root/.agent-coop
    environment:
      - ROOM_1_SECRET=${ROOM_1_SECRET:-}
    restart: unless-stopped

volumes:
  agent-coop-data:
