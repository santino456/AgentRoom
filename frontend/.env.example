# Vite environment variables for Agent Coop frontend
# Copy to .env and customize as needed

# API base URL (default: same-origin, e.g. '' when served by backend)
# VITE_API_BASE_URL=http://localhost:8080/api

# WebSocket base URL (default: derived from current host)
# VITE_WS_BASE_URL=ws://localhost:8080
